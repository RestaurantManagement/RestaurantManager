<li class="list-group-item">
  <div>
  <% menu_item = MenuItem.find(order_item.menu_item_id) %>
  <p><%= image_tag menu_item.image_url(:thumb).to_s %></p>
  <p><%= menu_item.name %></p>
  <p><%= menu_item.price %>$</p>
  <p><%= order_item.quantity %></p>
  <p class="last-p"><%= order_item.price %>$</p>
  </div>

</li>